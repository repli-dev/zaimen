# Local development keeps data in /data (gitignored).
# On Vercel, add a Neon Postgres database so POSTGRES_URL is set,
# then set SESSION_SECRET to a long random string.

SESSION_SECRET=
DATABASE_URL=
POSTGRES_URL=
